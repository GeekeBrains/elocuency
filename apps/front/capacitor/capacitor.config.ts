import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'elocuency.com',
  appName: 'elocuency',
  webDir: '../../../.tmp/apps/front/capacitor',
};

export default config;
